# Warden — 17-second v12 launch film

Current edit for X: **1920 × 1080, 30 fps, 510 frames, 17 seconds**.

The film cold-opens on a confidential file hitting the active policy boundary at 0.30s. The camera pulls back to show “Blocked by Warden”; public work crosses the other route. A business instruction becomes draft rules that a person reviews and activates. Claude Code and Codex connect to the same policy, and the next request demonstrates the check before the model. The official extruded 3D shield settles into one brand lockup with an anchored free download action.

## Source and exports

`index.html` owns the output clock, loading `assets/js/hook-v12.js`, `assets/js/product-workbench-v12.js` and the approved `end-card.js` / `end-logo-3d.js`. Motion comes from a physical file stop, a fan of rule planes docking into reviewable rows, cascading activation switches, connected tool surfaces and a packet moving through the check. The 17s edit is new choreography, not a global speedup of v11.

- `renders/warden-launch-17s-v12.mp4` — narrated master.
- `renders/warden-launch-17s-v12-silent.mp4` — silent companion.
- `renders/warden-launch-17s-v12-poster.png` — poster.

`production/v12-motion.json` records scene clocks. `production/FINAL-QA.md` records final verification once export completes. V11 remains in the historical export paths and `production/index-v11-before-v12.html`.

## Voice and sound

**Lukas – Excited youthful young man**, voice ID `wSqOdjeNqDrHcoK0zorF`, Eleven v3. The selected first take is 13.84s at its original rate and pitch. The full source is placed in five sentence segments, leaving short spaces for visible object contacts; narration ends 16.03s. Exact source cuts and output positions are in `production/v12-sound-plan.json`. The second completed take is retained. Two of the default four generations failed because the account permits two concurrent generations; ElevenLabs reported no charge for those failures.

The script was written first using the supplied anti-ai-slop writing skill: a concrete blocked-file event, explicit human review/activation, named tools, a scoped request check and a direct free/open-source CTA. `production/voiceover-v12-en.txt` contains the exact words. Flow: `pCGBY90nZz4ds3l5t5PZ`.

`assets/audio/warden-launch-v12-mix.wav` is the sole composition audio source. It adds twelve short object contacts edited from two verified native ElevenLabs effects and a quiet 120 BPM pulse edited from the existing original local composition. The bed is not claimed to be ElevenLabs music. There is no fresh sound generation, generic whoosh bed, voice pitch change or voice acceleration. The reproducible builder is `scripts/build-v12-mix.py`; the source/provenance/levels report is `production/sound-design-v12.json`.

## Product scope

The UI is an authored product example. A manager describes a business goal; Warden drafts rules, which need human review and activation. The examples do not promise a fixed number of rules. The illustrated check applies to connected requests and configured policies, not every application or all email traffic. No universal detection guarantee or fabricated incident is asserted. Claude Code and Codex are named accurately; “Codex CLI” is not used. Free/open-source positioning and macOS, Windows and Linux scope are owner-confirmed. The launch download goal is an aspiration, not a promised result.
