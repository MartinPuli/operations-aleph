# Warden v12 final media review

Verified master: `renders/warden-launch-17s-v12.mp4`, SHA256 `7c393d3a0d418f00a67acf127b6e53aad07525fc05287db718f18db7b8fd6aad`.

The export is exactly 17.000 seconds and 510 frames at 1920×1080 and 30 fps. Video is H.264, yuv420p and BT.709; audio is 48 kHz stereo AAC. A matching silent companion and opening poster were generated. `v12-media-probe.json` and `v12-artifacts.json` record the media properties and file identities.

The final thirteen-time source check passed with no lint, runtime, layout error or layout warning. Six DOM contrast checks passed. Two informational findings occur while the download label rises inside its intentional clipping container. Canvas text is outside automated contrast coverage. The hook and workbench modules passed deterministic seek/reseek checks independently.

Twelve source snapshots and a thirteen-frame encoded contact sheet were visually inspected. A final transition review at 7.22, 9.77 and 11.98 seconds found and corrected a compressed heading, mixed headings and redundant copy. The final angled wipe keeps one complete heading above the moving interface; opaque interface panels do not double-expose. The continuous outro retains one official extruded 3D shield, one wordmark and one anchored download action.

The selected Lukas source is 13.84 seconds and remains at its generated speed and pitch. Its complete audio is placed once across five sentence segments, with measured silent boundaries reserving time for the visible actions. Dialogue ends at 16.03 seconds. Source and stem reconstruction checks pass, and all native contact peaks align to their visual anchors within seven microseconds.

The final encoded audio measures −16.53 LUFS, −4.55 dBTP and 3.2 LU LRA. It correlates 0.999682 with the approved mix at zero-sample lag. The extra 128 decoded samples are AAC padding beyond the 17.000-second stream. The final 20 milliseconds measure −97.71 dBFS RMS, so the musical release ends cleanly. `v12-audio-qa.json`, `sound-design-v12.json` and `v12-encoded-audio-qa.json` record the checks.

The film demonstrates an authored product flow: a connected request is blocked by an active configured rule; a manager describes a goal; Warden drafts rules for human review and activation; Claude Code and Codex connect to that policy; a confidential request stops before the model while an allowed public request completes. The copy makes no fixed rule-count, universal detection or fabricated incident claim.

The landing references this 17-second master with cache key `v=12`, the matching poster and an updated duration label. The master copied to `landing/assets/launch/launch.mp4` has the same SHA256 as the verified render. All eleven landing analytics tests pass.

Verification covers deterministic rendering, sampled visual review, media structure, timing, loudness and source correspondence. It does not claim continuous subjective listening approval.
