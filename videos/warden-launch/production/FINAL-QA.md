# Warden v11 final media review

Verified master: `renders/warden-launch-22s-v11.mp4`, SHA256 `8a27509d3d240c2dfa463e3e0ad6ffe4eb77b3b9c326170193b82447391f2922`.

21.900 seconds, 657 frames, 1920×1080, 30fps, H.264/yuv420p, BT.709, AAC48kHz stereo. A matching silent companion and poster at2.8s are exported. `v11-media-probe.json` and `v11-artifacts.json` record the exact media and file identities.

The twelve-time source check passed with no lint, runtime or layout findings. Six DOM text contrast checks passed. Canvas text is outside that automated coverage, and the motion analyzer was not enabled. The workbench separately passed seven deterministic seek/reseek checks across section changes. Eleven source-frame captures and two confirmation captures were inspected alongside13 encoded montage samples. The first-frame camera scale was reduced from1.28 to1.04 to keep both file labels and Workspace inside frame. One duplicate small shield was removed. The corrected opening and final20.8s end card were visually confirmed.

An initial hardware-GPU snapshot process closed after the18.5s sample; the missing final samples and the complete final render succeeded with software WebGL. The final export uses one worker and the existing official 3D outro. There are no cartoon figures or cream/full-red teaching slides in the current composition. Product controls show drafts before human activation; connected AI tools and private/public outcomes remain legible in the sampled frames. This is sampled visual review, not a continuous human viewing claim.

Encoded audio: −17.15LUFS, −1.25dBTP,5.0LU LRA. The decoded master correlates0.999365 with the new Liam mix at zero lag; correlation to the old Derek mix is0.006403. The full20.64s source is retained with no tempo/pitch change and no truncated final speech. The mix uses eight existing native ElevenLabs effects; no old narration or score is included. `v11-encoded-audio-qa.json` and `v11-mix-verification.json` record those measurements. ASR and waveform verification confirm content/alignment, not subjective vocal preference.

The landing now references this21.9s master with a new poster and22-second label. A CSS-only restraint pass reduces decorative green, leaving Download, activation and meaningful outcomes distinct. Desktop and390×844 mobile views were visually inspected; mobile scrollWidth equals390. The cardless hero prompt and Blocked by Warden line remain. All11 landing analytics tests pass.
